# values.yaml 은 helm 이 실제로 먹는 파일이므로 추적한다 (CONTEXT.md "권위 있는 사본" 참조).
# 시크릿이 실제로 필요한 컴포넌트가 생기면 그 컴포넌트만 여기에 명시적으로 추가할 것.

# OS / editor 잡파일
.DS_Store
.idea/
.vscode/
*.swp

# Talos 인증 / kubeconfig (실수로라도 들어가면 안 됨)
talosconfig
kubeconfig
*.kubeconfig

# 작업 부산물
PLAN.md
*.bak
